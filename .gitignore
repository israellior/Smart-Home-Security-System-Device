node_modules/
npm-debug.log*

build/
build-*/
compile_commands.json
.cache/

# Machine-local editor state: it holds an absolute path to one home directory.
.vscode/

# Produced by git archive when shipping a copy to the Pi.
*.tar.gz

# Generated on the Pi; the README has the command. Not shipped.
*.wav

__pycache__/

# Clips waiting to be uploaded. Local to whichever machine recorded them.
spool/

# Where the deleted tools/clip-stub.js put what it was sent. Kept ignored so an
# old working copy does not suddenly offer to commit a pile of test clips.
received/
